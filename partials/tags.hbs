{{#if tags}}
  {{#foreach tags}}
    <a href="{{url}}" title="{{name}}" class="tag tag-{{id}} {{slug}} font-semibold rounded-md bg-gray-700 hover:bg-gray-900 text-white text-xs px-2 py-1 mr-2">{{name}}</a>
  {{/foreach}}
{{/if}}
